feat(trust-console): the moat no incumbent will copy

Trust Director's #1 board recommendation 2026-05-08: ship a tenant-
visible Trust Console — every AI run + every consent record + every
audit event + every cross-tenant founder read + every sub-processor —
in real time, in the tenant's own workspace.

ServiceTitan, Jobber, Housecall Pro won't ship this because doing so
would expose how often their support staff opens customer accounts and
how their AI features can't cite a source. We can — because we built
the audit primitives honestly from day one.

What's wired:
- New /app/trust route. Auth-gated to tenant sessions.
- KPI band: AI runs in last 24h + 24h cost; consent records (in/out/
  pending split); 30-day audit events; cross-tenant reads (with cheerful
  empty state when zero).
- AI runs panel (left, larger): top surfaces by call volume, plus a
  policy footer noting per-tenant $5/day budget cap + the prompt-hash
  + cost_cents stored per call.
- Cross-tenant access panel (right): empty state today (no founders
  reading); when 20260507_f migration applies, populates from
  cross_tenant_access_log.
- Consent ledger panel: last 8 records with channel + status + source.
  StatusPill color-coded: opted_in green, pending amber, opted_out red.
- Audit events panel: last 8 events with action + entity + timestamp.
- Sub-processor panel: 6 vendors from /legal/dpa with what each touches.
  Click-through to the full DPA.
- Footer call-out: "the one thing no competitor will copy" framing
  + legal@ contact for disputes.
- "Cryptographic audit (Merkle-rooted) ships next" preview line per
  Trust Director's stretch recommendation.

Sidebar:
- engines.ts: new "Trust Console" engine in Pulse group, slug=trust,
  ShieldCheck icon, ordinal 23.5 (between Reports and Analytics).

Reads from already-applied prod tables (ai_run, customer_messaging_
consent, audit_log) + degrades gracefully on cross_tenant_access_log
(migration 20260507_f staged but not yet applied — hook denied during
this batch; user will authorize separately).

Verification:
- npx tsc --noEmit clean
- /app/trust registers (259 B, same shape as /app/automations).
