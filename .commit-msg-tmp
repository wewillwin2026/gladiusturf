feat(post-tier4): customer detail vertical-aware + support_access_grants

Two follow-on items after the Tier 4 batch:

Customer detail page → vertical registry (Engineering Director's "before
vertical #2" keystone):
- app/app/(authed)/customers/[id]/page.tsx no longer hardcodes
  lighting_fixtures for non-lighting tenants. The fixtures rollup section
  now renders only for tenants where session.tenant.vertical === "lighting";
  irrigation/pool/etc tenants see a placeholder ("per-asset detail for the
  {vertical} vertical ships in a follow-up"). The customer profile, plan
  picker, and visit schedule render unchanged for every vertical.
- The query for lighting_fixtures is gated behind isLighting so non-
  lighting tenants don't pay for it. Other tenants can land on this page
  without 500ing.

support_access_grants schema + helper (board Decision 4 — REBRAND
founder cross-tenant access to opt-in):
- supabase/migrations/20260507_f_support_access_grants.sql:
    support_access_grants(tenant_id, granted_to_email, scope, status,
      expires_at, created_at, created_by_email, revoked_at,
      revoked_by_email)
    cross_tenant_access_log(tenant_id, actor_email, surface, action,
      resource_id, reason, granted_by_id, created_at)
  Both tenant-scoped via is_tenant_member RLS so tenants can read their
  own grants + access log when the future UI ships.
- lib/app/support-access.ts: logCrossTenantRead() — best-effort append
  to the access log, looks up the active grant if one exists and
  references it by FK. hasActiveGrant() helper for the future opt-in UI.
- Migration NOT YET APPLIED — user authorized a/b/c/d/e but not this
  one. Apply via psql when ready; the helper degrades gracefully (logs
  a warning, doesn't throw) until the tables exist.

Verification:
- /security rewrite probed live (commit 5268468 deploy): "App-layer",
  "magic-link auth", "Built honestly" copy live; "Postgres RLS" only
  appears in the migrating-roadmap context.
- /app/automations/storm + /app/automations 307 to login — auth gate
  intact, no 500s on the new routes.
- /api/cron/inventory-aging authed → tenantsNotified:1, totalUnits:6
  (post-/security rewrite, no regression).
- /legal/dpa 200 OK (74 KB).
- Homepage shows "Twelve engines" copy live; old "Thirty-three" gone.
- npx tsc --noEmit clean. npm run lint clean. npm run build clean.

USER ACTION OUTSTANDING:
- Apply 20260507_f_support_access_grants.sql to live Supabase via psql
  when ready. Until then, support-access.ts logs a console warning and
  no-ops on every founder cross-tenant query — no functional regression.
