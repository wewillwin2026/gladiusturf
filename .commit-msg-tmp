feat(board-day-3): AI budget cap + sidebar regroup + price raise + justification panel

User authorized board-deferral execution (afk for ~1hr at gym). Acted on
the board's strongest cross-cutting recommendations from the Day-2 + Day-3
synthesis without pausing. Memory updated.

AI budget cap (AI Director's "gating bug" — must ship before Friday):
- lib/ai/audit.ts: new checkAiBudget(tenantId) sums today's ai_run.cost_cents
  in a rolling 24h window. Hard cap $5/day per tenant (env-overridable via
  AI_DAILY_BUDGET_CENTS). Demo + founders sessions bypass.
- /api/ai/chat + /api/ai/quote: budget gate runs BEFORE the Anthropic call.
  When exceeded, streams a friendly explainer with the spent number + reset
  policy + founders@ contact. Closes the "one bored tenant on the OSHA
  manual = $40 in an afternoon" failure mode.
- Best-effort: fails open on lookup error. Audit row still writes either
  way.

Sidebar regroup — Sell / Field / Loop (5+ directors converged):
- components/app/engines.ts: ENGINE_GROUPS rewritten from
  Overview/Customers&Sales/Ops/Money/Intelligence/Platform to
  Today/Customers/Sell/Field/Money/Loop/Pulse/Platform. The three merges:
    * Sell  = Quotes + AI Quote Drafter + Plans + Pricing tables
    * Field = Schedule + Jobs + Routes + Territory + Crew + Equipment + Inventory
    * Loop  = Inbox + Reviews + Referrals + Campaigns + Automations + Ask Gladius
  Pulse (Reports + Analytics) stays separate from Loop — metrics ≠ messages.
  All slugs unchanged — every URL still resolves.

Price raise + justification panel (Strategy + Buffett + Jobs argued up,
Engineering + AI argued meter — we shipped both):
- content/pricing.ts: $397 → $597 / $997 → $1,497 / $2,997 → $3,997.
- All TIER_FEATURES, demo-form, demo API responses, comparison table,
  competitors.ts whySwitch updated to the new floors.
- Bright Lights Landscape Lighting (slug=bright-lights-encina) explicitly
  grandfathered at $397 in the pricing.ts header comment per Strategy's
  "never raise mid-pen" principle. Per-tenant Stripe override pattern
  ships when payment routing lands.
- New "Why $597" justification panel on /pricing between TIERS and
  BDC_ADDON: 4 cards (vs LMN $2,392/mo at 4 seats, vs Aspire 102% YoY,
  vs ServiceTitan $46K ETF, vs per-user nickel-and-dime), plus a
  champagne-tinted "what you're paying for at $597" benefits ribbon
  with the $14,200 Quote Intercept payback math (one catch = 2yr ROI).
- Drops "Founding cohort, 12 of 20 slots" theater across pricing-section,
  hero, social-proof-strip per board's unanimous "drop unless 8 are
  signed paper" call. Replaced with "Annual increase capped at 5%" promise
  (Product's price-ceiling line — Aspire's 102% YoY hike is the foil).
- Page metadata, scarcity band, hero proof strip all updated to lead
  with the cap promise instead of the countdown timer.

Pricing followup tracked in memory — next board review will be asked to
defend each new floor against named competitor specifics for a /pricing
"justification packet" page + a CFO-grade sales handout.

Verification:
- npx tsc --noEmit clean
- npm run lint clean
- npm run build clean
