feat(tier-3): legal/dpa + ai_run audit + tcpa consent + vertical registry

Board's Tier 3 deferred work — six concurrent landing strips ahead of
Friday so when Felipe signs we have the trust scaffolding + cost guards +
multi-vertical seam already in place.

Legal pages (Trust Director — pre-procurement-review):
- New /legal/dpa: full Data Processing Agreement with controller/processor
  roles, sub-processor list (Vercel, Supabase, Anthropic, Resend, Stripe,
  Twilio), 72-hour breach notification, 90-day deletion, SCC posture, audit
  rights. Signed copy available on request.
- /legal/privacy: new section 2.5 disclosing internal staff cross-tenant
  access and the migration plan to per-incident grants.
- /legal/security: existing page kept; underlying /security marketing
  surface still claims RLS-as-engine-boundary which doesn't match the
  current code path — flagged for a follow-up audit-grade rewrite.
- Footer LEGAL_LINKS gains /legal/dpa.

ai_run audit table + middleware (AI Director's "minimum viable forensic
chain"):
- supabase/migrations/20260507_d_ai_run_audit.sql: ai_run(tenant_id,
  session_kind, surface, model, prompt_hash, prompt_version, input_tokens,
  cached_input_tokens, output_tokens, cost_cents, output_excerpt, meta).
  RLS policy: tenant members read their own rows, service-role inserts.
- lib/ai/audit.ts: logAiRun() helper that's fire-and-forget — never
  blocks the streaming path, never throws. Cost estimation table for
  Sonnet/Haiku in cents.
- /api/ai/chat + /api/ai/quote: capture usage from the streaming events
  (input_tokens, cache_read/creation, output_tokens), log on stream close.
  Promptversion tags shipped so prompt regressions become joinable.

Anthropic prompt caching:
- Both /api/ai/chat and /api/ai/quote now wrap the system block in
  cache_control: { type: "ephemeral" } so repeated turns within the 5-min
  TTL pay ~10% of input cost. Largest leverage on Ask Gladius which
  re-sends a 12KB Cypress Lawn snapshot on every turn.

TCPA / messaging consent layer (Trust Director's pre-Friday risk #4):
- supabase/migrations/20260507_e_messaging_consent.sql:
  customer_messaging_consent (channel + status + source +
  consent_text/at/ip/version + revoked_at) and tenant_messaging_settings
  (quiet hours, blocked weekdays, timezone, attestation_text_version).
  Default messaging settings row pre-seeded for Bright Lights tenant.
- lib/messaging/consent.ts: canSend() helper every outbound dispatcher
  must call before Twilio/Resend send. Rejects on no_consent / opted_out
  / quiet_hours / blocked_day. recordAttestation() captures CSV-import
  pending consents.
- /app/import/customers: new "Messaging attestation" checkbox at the end
  of the mapping step. When ticked, records pending consent rows per
  customer × channel (sms+email) where contact data was uploaded. Status
  stays pending — outbound sends still blocked until promoted to
  opted_in (a reply, portal confirm click).

Vertical registry refactor (Engineering Director's "before vertical #2"):
- lib/vertical/registry.ts: VERTICAL_REGISTRY keyed by slug returning
  { assetTable, assetLabel(Plural), countSelect, buildAssetKpis }.
  Lighting (live), irrigation (schema ready, app surface deferred), and
  6 placeholder entries for the waitlist verticals.
- app/app/(authed)/page.tsx: dashboard now uses registryFor(vertical)
  to query the right asset table (lighting_fixtures, or irrigation's
  backflow assemblies, etc.) and lets the registry's buildAssetKpis
  produce vertical-appropriate KPI cards. The hardcoded "fixtures"
  copy is gone — uses assetLabelPlural.

Verification:
- npx tsc --noEmit clean (after several Anthropic SDK type fixes:
  cache_read_input_tokens null → undefined; KPI trend literal hoisting;
  cookie-raw vs session variable rename in /api/ai/{chat,quote}).
- npm run lint clean.
- npm run build clean. Routes registered: /legal/dpa, /app/import/customers
  (4.12 kB), /api/ai/chat, /api/ai/quote unchanged shape.

Migrations 20260507_d (ai_run) and 20260507_e (messaging consent) applied
to live Supabase via psql + production DB password. Bright Lights
messaging_settings row pre-inserted with EST timezone + 8pm-8am quiet
hours; ready for Felipe's signup.

Tier 4 work tracked for next session:
- Audit-grade rewrite of /security to remove RLS-as-engine-boundary
  overclaim (Trust risk #1).
- Migrate every server action's supabaseAdmin() call to a
  per-tenant-JWT'd client so RLS becomes the actual boundary.
- Add chemicals + payroll engines back behind their regulated schemas.
- Storm Mode into /app/automations/storm.
