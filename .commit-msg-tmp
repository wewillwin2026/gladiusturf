feat(transparency): cryptographic Merkle audit roots + pricing CFO panel + support_access_grants migration applied

Three board-mandated wins ratified by user "yes to all three":

1) support_access_grants + cross_tenant_access_log migration applied to
   live Supabase via psql. Both tables live + empty. Privacy §2.5 founder-
   access disclosure now has enforceable code behind it. Helper
   `lib/app/support-access.ts:logCrossTenantRead` already wired; first
   founder cross-tenant read will write a row + flow into the Trust
   Console's "Founder cross-tenant access" panel.

2) Cryptographic Merkle audit roots — Trust Director's stretch ask.
   - lib/audit/merkle.ts: pure SHA-256 Merkle tree over the audit chain
     (ai_run + audit_log + cross_tenant_access_log +
     customer_messaging_consent). Deterministic row canonicalization
     (alpha-sorted JSON-stringified key=value pairs, table-prefixed,
     leaf-sorted lexicographically before folding). Standard binary
     Merkle with last-leaf-duplication on odd-count layers.
   - /api/transparency/root/[date]: GET endpoint returning JSON
     {date, tenantId, rootHex, counts, computedAt}. Tenant session →
     tenant-scoped root; Bearer CRON_SECRET → all-tenants root for
     engineering+legal forensics.
   - /transparency: public page explaining how anyone can re-compute
     the root, what it proves (silent row mutation breaks the day's
     hash), what it doesn't (we still need to publish honestly — daily
     public-notary timestamping is v2).
   - /app/trust footer line replaced "ships next" placeholder with live
     /transparency link + the API endpoint.
   - Footer LEGAL_LINKS: added "Transparency" tab.

3) Pricing CFO one-pager panel on /pricing (Strategy + Buffett
   pricing-justification round delivered the content).
   - 4 ranked bullets: "competitor floor is a lie" / "seasonal hires
     are free" / "no ETF, no implementation fee, no rollup tax" /
     "revenue intelligence pays for the seat." Each grounded in the
     numbers from content/competitors.ts.
   - Buffett's "the new combine" parable as the closing block.
   - Note: Jobs' justification round hit a transient API rate limit;
     will dispatch separately when needed for an even-tighter
     positioning panel.

Verification:
- npx tsc --noEmit clean
- npm run lint clean
- Build registers /transparency (231 B static), /api/transparency/root/
  [date] (200 B dynamic), /pricing static + new panel inlined
