chore(copy): drop "Founding cohort" theater from FAQ + Council + Contact

Smoke test of commit 613ee43 deploy caught three remaining "Founding
cohort" / "12 of 20 slots" instances that escaped the 0e896b3 sweep:
- components/home-faq.tsx — FAQ answer "Do you have customers in my
  service area?" claimed cohort spans East Coast / Midwest / Texas
  with N=1 paying customer. Replaced with honest "Sarasota lighting
  business is our first paying customer" framing.
- app/contact/page.tsx — "Founding cohort: Direct Signal thread"
  bullet → "Founder-direct: Signal thread."
- app/council/page.tsx — "Founding cohort · 12 of 20 May 2026 slots
  remaining" pulse line → "Annual increase capped at 5%, in writing."
  Page metadata description + OG description also stripped of the
  cohort phrase.

Buffett's hardest line stays the test: "12 of 20 slots remaining is
theater unless 8 of those slots are signed paper. If they are, say their
names. If they aren't, take the counter down — a phony scoreboard is
the fastest way to lose a customer who can count."

Verification: npx tsc clean, npm run build clean, no other "Founding
cohort" strings in TS/TSX outside docs/audit-* (which are the audit
docs themselves).
